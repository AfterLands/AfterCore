package com.afterlands.core.input;

/**
 * Tipos de input disponíveis.
 */
public enum InputType {
    /** Captura texto digitado no chat. */
    CHAT,
    /** Captura texto de uma placa virtual (requer ProtocolLib). */
    SIGN,
    /** Captura texto renomeado em uma bigorna. */
    ANVIL
}
